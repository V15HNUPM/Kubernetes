apiVersion: v1
kind: Service
metadata:
  name: expenses-app-service
  namespace: springboot-expenses
spec:
  selector:
    app: expenses-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
